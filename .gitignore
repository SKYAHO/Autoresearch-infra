# OS / editor
.DS_Store
.idea/
.vscode/
__pycache__/
.venv/

# Local-only agent settings (.claude/docs/ is committed)
.claude/*
!.claude/docs/
agent.local.md
docs/NOTION_PROGRESS_TIMELINE.md
docs/TROUBLESHOOTING_2026-07-16-session.md

# Local env / secrets
keys/
.env
.env.*
!.env.example
*.pem
*.key
*.json.key
*service-account*.json

# Terraform / IaC
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
*.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Git worktrees (local)
.worktrees/

# repo-walk 로컬 캐시
.repo-walk/

# GCP local files
application_default_credentials.json
gcloud-config/

# #183 charts/ tgz는 vendor 안 함(ArgoCD가 build). 단 Chart.lock은 커밋한다
# — ArgoCD의 `helm dependency build`가 lock을 요구하기 때문(없으면 sync 실패).
deploy/*/charts/
